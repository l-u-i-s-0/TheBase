-- 1. Ver el segmento de undo de la transacción
SELECT s.sid, t.xidusn, t.used_ublk, t.used_urec,
       r.status, r.name
FROM   v$session     s
JOIN   v$transaction t  ON t.addr = s.taddr
JOIN   v$rollname    r  ON r.usn  = t.xidusn
WHERE  s.sid = 3532;
